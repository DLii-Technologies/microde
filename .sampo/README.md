# Sampo

Release intent is recorded in `.sampo/changesets/`.

Add release intent with `pnpm sampo add`, preview pending releases with
`pnpm sampo release --dry-run`, prepare a release with `pnpm release:prepare`,
and publish with `pnpm release`.
