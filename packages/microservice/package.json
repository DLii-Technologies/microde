{
	"name": "@microde/microservice",
	"version": "0.0.1",
	"description": "Composition and lifecycle driver for long-running microservices.",
	"license": "MIT",
	"author": "DLii Technologies",
	"repository": {
		"type": "git",
		"url": "https://github.com/DLii-Technologies/microde.git",
		"directory": "packages/microservice"
	},
	"keywords": [
		"microservice",
		"composition",
		"lifecycle"
	],
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.build.json && node ../../scripts/copy-license.mjs",
		"prepack": "pnpm run build",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"typecheck": "tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "^26.1.2"
	}
}
